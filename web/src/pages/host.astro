---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Host a Scoreboard">
  <div class="flex flex-col items-center w-full max-w-5xl mx-auto">
    <header class="w-full flex flex-col items-center text-center mb-10 border-b border-white/10 pb-6">
      <h1 class="text-4xl font-bold mb-4 text-white text-center">Free CTF Scoreboard</h1>
      <!-- Scoreboard Preview Image -->
      <!-- Scoreboard Preview Image -->
      <!-- Scoreboard Preview Image -->
      <div class="mb-8 w-[80%] mx-auto border-2 border-white/30 shadow-2xl shadow-cyber-primary/30 transform transition-transform hover:scale-[1.01] duration-500">
        <div class="absolute inset-0 bg-gradient-to-t from-cyber-darker/50 to-transparent pointer-events-none z-10"></div>
        <img 
          src="/scoreboard_preview.png" 
          alt="CTF Scoreboard Preview" 
          class="w-full h-auto object-cover opacity-95 hover:opacity-100 transition-opacity"
        />
      </div>
      <p class="text-lg text-slate-400 text-center">
        Ready to run a competition? Spin up your own scoreboard in seconds.
      </p>
    </header>

    <div class="flex flex-col items-center w-full space-y-8 md:block">
      
      <section class="cyber-card p-4 md:p-8">
        <h2 class="text-2xl font-bold text-white mb-4 text-center md:text-left">Run the Scoreboard</h2>
        <div class="prose prose-invert prose-sm text-slate-300 text-center md:text-left w-full mx-auto md:mx-0">
          <p>This command will start a CTF Scoreboard on port 4005.</p>
          
          <div class="mb-4">
            <pre class="bg-black/50 p-4 rounded-lg whitespace-pre-wrap break-all text-left"><code>docker run -d -p 4005:4005 joshbeck2024/ctf-cyberlessons-flag-scoreboard-public:latest</code></pre>
          </div>

          <div class="bg-blue-900/20 border border-blue-500/30 rounded-lg p-4 mb-4 text-center md:text-left">
            <h3 class="text-blue-400 font-bold mb-2">Management Credentials</h3>
            <p>Username: <code class="text-cyber-primary">admin</code></p>
            <p>Password: <code class="text-cyber-primary">admin</code></p>
          </div>
          
          <p class="mt-4">
            It is preconfigured with the flags on this platform, but you can delete them and start over. 
            Once running, access the scoreboard at <a href="http://localhost:4005" class="text-cyber-primary hover:underline">http://localhost:4005</a>.
          </p>
        </div>
      </section>

      <section class="cyber-card p-4 md:p-8 border-red-900/30">
        <h2 class="text-xl font-bold text-red-400 mb-4 text-center md:text-left">Cleanup</h2>
        <p class="text-slate-400 text-sm mb-4 text-center md:text-left">
          To stop and remove the scoreboard container:
        </p>
        <pre class="bg-black/50 p-4 rounded-lg whitespace-pre-wrap break-all text-left"><code>docker stop ctf-scoreboard && docker rm ctf-scoreboard</code></pre>
        <p class="text-slate-500 text-xs mt-2 italic text-center md:text-left">Note: Commands may vary based on your container name/ID.</p>     
      </section>

    </div>
  </div>
</Layout>
